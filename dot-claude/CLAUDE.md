We're running node v22 which runs TypeScript natively

When reading PR review comments (esp. from bots), validate the claim against the code before fixing. Push back when it's wrong.

Never use my real name unless I specifically ask you to.
